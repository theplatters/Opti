function [outputArg1,outputArg2] = testFunction(N)
n = N - 1;
e = ones(n,1);
G = spdiags([-e, 2*e, -e],-1:1,n,n);


d = 3 * ones(n,1);

A = eye(n)

b1 = 0.1 * ones(N/3,1)
b2 =  -0.2 * ones(N/3,1)
b3 = 0.05 





end

